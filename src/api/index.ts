import { apiBaseUrl } from '@/config';

export const urlPatients = apiBaseUrl + 'patients/';

export const urlPatientManagers = apiBaseUrl + 'patient-managers/';

export const urlAppointments = apiBaseUrl + 'appointments/';
